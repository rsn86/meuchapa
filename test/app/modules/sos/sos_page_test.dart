import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:meuchapa/app/modules/sos/sos_page.dart';

main() {
  testWidgets('SosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SosPage(title: 'Sos')));
    //  final titleFinder = find.text('Sos');
    //  expect(titleFinder, findsOneWidget);
  });
}
