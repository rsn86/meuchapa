import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:meuchapa/app/modules/recompensas/recompensas_page.dart';

main() {
  testWidgets('RecompensasPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RecompensasPage(title: 'Recompensas')));
    //  final titleFinder = find.text('Recompensas');
    //  expect(titleFinder, findsOneWidget);
  });
}
