import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:meuchapa/app/modules/recompensas/recompensas_controller.dart';
import 'package:meuchapa/app/modules/recompensas/recompensas_module.dart';

void main() {
  initModule(RecompensasModule());
  // RecompensasController recompensas;
  //
  setUp(() {
    //     recompensas = RecompensasModule.to.get<RecompensasController>();
  });

  group('RecompensasController Test', () {
    //   test("First Test", () {
    //     expect(recompensas, isInstanceOf<RecompensasController>());
    //   });

    //   test("Set Value", () {
    //     expect(recompensas.value, equals(0));
    //     recompensas.increment();
    //     expect(recompensas.value, equals(1));
    //   });
  });
}
